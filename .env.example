# Backend
PORT=3001
NODE_ENV=development

# Frontend (build-time, for local dev without proxy)
# When using docker-compose, leave unset - nginx proxies /api to backend
VITE_API_URL=http://localhost:3001
